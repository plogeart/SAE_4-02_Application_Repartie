<?xml version="1.0" encoding="UTF-8"?>

<project name="sae-application" default="compile" basedir=".">
    
    <property name="src.dir" value="src"/>
    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="lib.dir" value="lib"/>

    <path id="project.classpath">
        <fileset dir="${lib.dir}">
            <include name="**/*.jar"/>
        </fileset>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="compile" depends="clean">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir}" destdir="${classes.dir}" includeantruntime="false">
            <classpath refid="project.classpath"/>
        </javac>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${build.dir}/jar"/>
        <jar destfile="${build.dir}/jar/proxy-server.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="com.example.proxy.ProxyServer"/>
                <attribute name="Class-Path" value="../lib/json-20131018.jar"/>
            </manifest>
        </jar>
        <jar destfile="${build.dir}/jar/restaurant-server.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="com.example.server.RestaurantServer"/>
                <attribute name="Class-Path" value="../lib/json-20131018.jar"/>
            </manifest>
        </jar>
        <jar destfile="${build.dir}/jar/restaurant-client.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="com.example.client.RestaurantClient"/>
                <attribute name="Class-Path" value="../lib/json-20131018.jar"/>
            </manifest>
        </jar>
    </target>
</project>